import 'package:flutter/material.dart';

class IronDeficiency extends StatelessWidget {
  const IronDeficiency({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body:Text("IronDeficiency") ,
    );
  }
}