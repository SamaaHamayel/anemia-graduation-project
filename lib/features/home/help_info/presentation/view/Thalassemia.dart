import 'package:flutter/material.dart';

class Thalassemia extends StatelessWidget {
  const Thalassemia({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text("Thalassemia"),
    );
  }
}